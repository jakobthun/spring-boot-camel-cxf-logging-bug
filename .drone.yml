image: ipaasio/java8-ci-build
script:
  - gradle build
deploy:
  ssh:
    target: core@46.101.154.202:/home/core
    artifacts:
      # Jar and deployscript will be zipped and put to /home/core
      - build/libs/*.jar
      - deploy.sh
    cmd: ./deploy.sh
notify:
  email:
    recipients:
      - jakob.thun@gmail.com