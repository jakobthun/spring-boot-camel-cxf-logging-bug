image: ipaasio/java8-ci-build
script:
  - gradle build
deploy:
  ssh:
    target: core@46.101.154.202:/home/core
    artifacts:
      - build/libs/*.jar
    cmd: docker run -d -v /home/core/\$(ls -1rt | grep .*jar$ | tail -n 1):/data/app.jar dockerfile/java:oracle-java8 java -jar /data/app.jar
notify:
  email:
    recipients:
      - jakob.thun@gmail.com
#